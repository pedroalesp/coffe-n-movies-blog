module.exports = {
    siteUrl: `http://localhost:8000`, // Site domain. Do not include a trailing slash!

    postsPerPage: 3, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `Coffee n' Movies`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `Opiniones, críticas, reviews y recomendaciones de las mejores películas. El sitio donde hablamos de cine sin saber de cine.`, // This allows an alternative site description for meta data for pages.

    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `Coffee n' Movies`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `favicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#e9e9e9`, // Used for Offline Manifest
    themeColor: `#15171A`, // Used for Offline Manifest
}
